# Frontend Environment Variables
# Note: Google Maps API key is now handled securely through the backend
# No API keys should be exposed in frontend environment variables

# Backend API URL (for development)
VITE_API_URL=http://localhost:5000 